import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div className="flex h-full flex-1 items-center justify-center">
      <Typography variant="h6">Select a workflow to continue</Typography>
    </div>
  );
};

export default Home;
